import { createStore } from 'vuex'

const state = {
    $tim: null,
};

const actions = {
}

export default createStore({
  state,
  getters: {
  },
  mutations: {
  },
  actions,
  modules: {
  }
})
