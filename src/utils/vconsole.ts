// 注释掉，需要的时候再打开
// export default () => {
//     if (window.location.port || window.location.hostname.indexOf('test') > -1) {
//         const VConsole = require('vconsole');
//         return new VConsole();
//     }
//     return '';
// };
export default () => '';
